apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: "redis-monitor"
  namespace: monitoring  # Prometheus namespace
  labels:
    release: prometheus-stack  # Matches Helm selector
spec:
  selector:
    matchLabels:
      app: redis-service  # Your Redis service label
  endpoints:
  - port: redis  # Your service port name (e.g., where /metrics is exposed)
    path: /metrics
    interval: 30s
  namespaceSelector:
    matchNames:
    - default  # Your app namespace